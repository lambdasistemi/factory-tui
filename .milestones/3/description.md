Outcome: the published factory-tui can be called good enough to release. No known release-blocking bugs remain. The browser does not lie about work in progress.

Observable test: a stranger installs the milestone artifact via Nix; `factory-tui --dump` does not mark waiting seats RUNNING; the configuration schema is published; the setup skill can select running-status samplers from the live box; every remaining finding is fixed or explicitly waived.

Artifact: milestone pre-release line that graduates into the next product release at close.

Scope: residual and release-blocking defects on the shipped tmux browser. Not M1 validation. Not tagging, packaging, or announcing a release (that is a later, separately authorized act). New behavior only when required to stop a false status.

State: https://github.com/lambdasistemi/factory-tui/wiki/M3-State
